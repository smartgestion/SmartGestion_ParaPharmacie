node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

.vercel
.sql

# Note: api/index.js is intentionally NOT ignored. It is the prebuilt
# Vercel serverless function bundle (produced by `npm run build:api`
# from scripts/api-entry.ts) and MUST be committed so that Vercel
# can discover it during function registration, which happens before
# the buildCommand executes.
